# The tests use the same ehcache config as the application
# but for some reason the path has to start with two slashes to make it work
# See also https://stackoverflow.com/questions/72200524/how-to-correctly-set-spring-jpa-properties-hibernate-javax-cache-uri
# (which currently does provide a solution)

spring.jpa.properties.hibernate.javax.cache.uri=classpath://ehcache.xml